name: common_domain

description: A new Flutter package project.

version: 0.0.1

publish_to: "none"

homepage:

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  common_dependencies:
    path: "../common_dependencies"

  common_design_system:
    path: "../common_design_system"

  mobx: 2.3.0+1
  flutter_mobx: 2.2.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
